{
  "name": "murdhocom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-styles": "tailwindcss -c resources/tailwind.config.js -i resources/app.css -o public/style.css",
    "build-assets": "cp -a static/. public",
    "build-html": "bb src/render.clj",
    "build": "npm run build-assets && npm run build-html && npm run build-styles",
    "build-prod": "npm run build && rm -rf docs && mv public docs && prettier --write docs/index.html",
    "watch": "watchexec -r -w src -w resources -w static npm run serve",
    "serve": "npm run build && bb server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "2.8.3",
    "tailwindcss": "^3.1.4"
  }
}
